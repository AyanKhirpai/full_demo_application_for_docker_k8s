apiVersion: v1
kind: Service
metadata:
  name: frontendend-service
  labels: 
    name: frontendend-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    name: frontendend-pod